import styled from 'styled-components/native';
import { colors } from '../../../colors';

export const MainWaitingText = styled.Text`
    color: ${colors.darkWhite};
    font-size: 17px;
    line-height: 20px;
`;